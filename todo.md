# RSS Reader Prototype — To-do

- [x] Replace the multiple Fetch and Fetch All buttons with a single Update Sources button represented by a Lucide circle-arrow icon. Place it immediately to the left of the Settings gear icon when the sidebar is expanded and above the gear icon when it is collapsed. Remove all other fetch buttons. Fetch sources automatically when the application opens if it does not already do so.

- [x] Collapse the Sources dropdown in the sidebar by default.

- [x] Automatically collapse the Collections and Sources dropdowns when the sidebar is collapsed. Do not allow either dropdown to be expanded while the sidebar is collapsed.

- [x] Make the Collections and Sources entries in the sidebar navigate to the existing Manage Collections and Manage Sources views. Remove the separate Manage section from the sidebar. Expanding and collapsing Collections and Sources should only happen when the user clicks the dropdown arrow or its nearby disclosure-control area.

- [x] Make collection icons customizable when creating or editing a collection. Let the user choose from a curated set of relevant Lucide icons, such as icons for business, technology, science, and nature, rather than exposing every Lucide icon.

- [x] In the Manage Collections overview, make clicking a collection card navigate to that collection, exactly as clicking the collection in the sidebar does. Similarly, make clicking a source in the Manage Sources view navigate to that source, exactly as clicking the source in the sidebar does.

- [x] Display each source's favicon as its icon. If no favicon is available, use the existing RSS signal icon as the fallback.

- [x] Change fullscreen article mode so the article opens in a new box with centered text. The box should cover the entire application area except the sidebar, including the current article area, article list, and top banner showing All Items or the selected source. Replace the text-labelled fullscreen button next to Mark Read with a Lucide expand/shrink icon in the top-right corner of the article box.

- [x] In the Manage Sources list, order the actions from left to right as Edit, Disable, and Delete.

- [x] Replace operating-system warning dialogs for destructive actions, such as deleting a single source or collection, with in-app confirmation prompts.

- [x] In Manage Sources selection mode for bulk deletion, make clicking anywhere on a source select or deselect it.
